#week 2 assignment
#express assignment
Set up a new branch express continuing from previous Node.js project
Installed and configured Express and Pug
Created dynamic HTML landing page rendered with Pug
Shows API info and available endpoints
Built REST API using Express router
Added mock in-memory data and CRUD endpoints:
GET /api/items — list items
GET /api/items/:id — get single item
POST /api/items — create item
PUT /api/items/:id — update item
DELETE /api/items/:id — delete item (with demo forbidden check)
Returns proper status codes and JSON responses
Serves static media files from /media